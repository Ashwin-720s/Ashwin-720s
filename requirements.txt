# CBSE Study Bot — dependencies
# Install with: pip install -r requirements.txt --break-system-packages
# (on your own PC, just: pip install -r requirements.txt)

# PDF text extraction
pymupdf==1.24.10

# Classic ML (this is what we ACTUALLY train)
scikit-learn==1.5.2
pandas==2.2.3
numpy==1.26.4
matplotlib==3.9.2
seaborn==0.13.2
joblib==1.4.2

# Embeddings + vector search (for retrieval, no training needed)
sentence-transformers==3.1.1
faiss-cpu==1.9.0

# Local LLM client (talks to Ollama, which you install separately)
ollama==0.3.3

# Chatbot front end
streamlit==1.39.0
